# 4.接口层 API 联调用例（curl + JSON）

## 1. 说明
- 基础地址：`http://127.0.0.1:8000`
- 启动服务：

```bash
/data/luntai/lzw_dev/code_common/ai_training_platform/.venv/bin/python -m uvicorn web.app:app --reload
```

- 本文示例以当前最小实现为准，主要用于联调联通性验证。

## 2. 健康检查

### 2.1 请求
```bash
curl -s http://127.0.0.1:8000/health
```

### 2.2 响应示例
```json
{
  "ok": true,
  "data": {
    "status": "up"
  }
}
```

## 3. 项目与 KPI 配置

说明：当前实现没有项目创建 HTTP 端点；项目需要在服务容器中预置后再调用 KPI 接口。

### 3.1 更新 KPI 配置
```bash
curl -s -X PUT "http://127.0.0.1:8000/projects/proj-1/kpi-config" \
  -H "Content-Type: application/json" \
  -d '{
    "primary_kpi": "business_kpi",
    "threshold": 0.8,
    "weights": {"eval": 0.7, "deploy": 0.3},
    "deploy_constraints": [
      {"metric": "latency_p95_ms", "operator": "<=", "value": 30}
    ]
  }'
```

成功响应示例：
```json
{
  "ok": true,
  "data": {
    "project_id": "proj-1",
    "primary_kpi": "business_kpi",
    "threshold": 0.8,
    "weights": {"eval": 0.7, "deploy": 0.3},
    "deploy_constraints": [
      {"metric": "latency_p95_ms", "operator": "<=", "value": 30}
    ]
  }
}
```

失败响应示例（项目不存在）：
```json
{
  "ok": false,
  "error": {
    "code": "NOT_FOUND",
    "message": "project not found",
    "details": [],
    "type": "business_error"
  }
}
```

### 3.2 查询 KPI 配置
```bash
curl -s "http://127.0.0.1:8000/projects/proj-1/kpi-config"
```

## 4. 数据集与场景标签

### 4.1 导入数据集版本
```bash
curl -s -X POST "http://127.0.0.1:8000/datasets/ds-1/versions/import" \
  -H "Content-Type: application/json" \
  -d '{"version": 1, "manifest_uri": "s3://bucket/v1.json"}'
```

成功响应示例：
```json
{
  "ok": true,
  "data": {
    "dataset_version_id": "ds-1:v1",
    "dataset_id": "ds-1",
    "version": 1,
    "manifest_uri": "s3://bucket/v1.json",
    "frozen": false
  }
}
```

### 4.2 冻结数据集版本
```bash
curl -s -X POST "http://127.0.0.1:8000/datasets/ds-1/versions/1/freeze"
```

重复冻结失败示例：
```json
{
  "ok": false,
  "error": {
    "code": "CONFLICT",
    "message": "dataset version already frozen",
    "details": [],
    "type": "business_error"
  }
}
```

### 4.3 写入场景标签
```bash
curl -s -X PUT "http://127.0.0.1:8000/datasets/versions/ds-1:v1/scene-labels" \
  -H "Content-Type: application/json" \
  -d '{
    "labels": [
      {
        "image_id": "img-1",
        "time_of_day": "day",
        "weather": "sunny",
        "environment": "outdoor"
      },
      {
        "image_id": "img-2",
        "time_of_day": "night",
        "weather": "rain",
        "environment": "outdoor"
      }
    ]
  }'
```

### 4.4 查询场景覆盖率
```bash
curl -s "http://127.0.0.1:8000/datasets/versions/ds-1:v1/scene-coverage?dimensions=time_of_day&dimensions=weather"
```

响应示例：
```json
{
  "ok": true,
  "data": {
    "dataset_version_id": "ds-1:v1",
    "dimensions": ["time_of_day", "weather"],
    "items": [
      {"dimension_key": "day+sunny", "count": 1, "ratio": 0.5},
      {"dimension_key": "night+rain", "count": 1, "ratio": 0.5}
    ]
  }
}
```

## 5. 任务编排与观测

### 5.1 创建任务
```bash
curl -s -X POST "http://127.0.0.1:8000/jobs" \
  -H "Content-Type: application/json" \
  -d '{
    "job_id": "job-1",
    "task_type": "training",
    "dataset_version_id": "ds-1:v1",
    "resources": {"gpu_count": 1}
  }'
```

### 5.2 查询任务状态
```bash
curl -s "http://127.0.0.1:8000/jobs/job-1"
```

### 5.3 启动任务
```bash
curl -s -X POST "http://127.0.0.1:8000/jobs/job-1/start" \
  -H "Content-Type: application/json" \
  -d '{"gpu_id": "0"}'
```

### 5.4 追加与查询日志
```bash
curl -s -X POST "http://127.0.0.1:8000/jobs/job-1/logs" \
  -H "Content-Type: application/json" \
  -d '{"message": "train step 1"}'

curl -s "http://127.0.0.1:8000/jobs/job-1/logs?page=1&page_size=20"
```

### 5.5 写入与查询指标
```bash
curl -s -X POST "http://127.0.0.1:8000/jobs/job-1/metrics" \
  -H "Content-Type: application/json" \
  -d '{"ts": 1000, "values": {"loss": 0.9, "lr": 0.001}}'

curl -s "http://127.0.0.1:8000/jobs/job-1/metrics?start_ts=900&end_ts=2000"
```

## 6. 分析产物查询

说明：以下接口依赖产物已注册。当前最小实现里没有公开产物写入端点，通常由后端流程注入。

### 6.1 查询评估报告
```bash
curl -s "http://127.0.0.1:8000/jobs/job-1/artifacts/eval-report?run_id=run-1"
```

### 6.2 查询证据包
```bash
curl -s "http://127.0.0.1:8000/jobs/job-1/artifacts/evidence-pack?run_id=run-1"
```

### 6.3 查询分析报告
```bash
curl -s "http://127.0.0.1:8000/jobs/job-1/artifacts/analysis-report?run_id=run-1"
```

### 6.4 查询下一轮实验建议
```bash
curl -s "http://127.0.0.1:8000/jobs/job-1/artifacts/next-experiments?run_id=run-1"
```

失败响应示例（产物不存在）：
```json
{
  "ok": false,
  "error": {
    "code": "NOT_FOUND",
    "message": "artifact not found",
    "details": [],
    "type": "business_error"
  }
}
```

## 7. Proposal 工作流

### 7.1 校验候选实验
```bash
curl -s -X POST "http://127.0.0.1:8000/proposals/validate" \
  -H "Content-Type: application/json" \
  -d '{
    "analysis_report": "summary",
    "next_experiments": {
      "experiments": [
        {
          "name": "exp-1",
          "changes": [{"field": "lr", "from": 0.001, "to": 0.0005}],
          "expected": "higher kpi",
          "evidence_refs": ["eval_report.overall.business_kpi"]
        }
      ]
    }
  }'
```

成功响应示例：
```json
{
  "ok": true,
  "data": {
    "accepted": 1
  }
}
```

### 7.2 确认后创建任务
```bash
curl -s -X POST "http://127.0.0.1:8000/proposals/create-from-candidate" \
  -H "Content-Type: application/json" \
  -d '{
    "candidate": {
      "name": "exp-1",
      "changes": [{"field": "lr", "from": 0.001, "to": 0.0005}],
      "expected": "higher kpi",
      "evidence_refs": ["eval_report.overall.business_kpi"]
    },
    "baseline_job_id": "job-base",
    "who": "alice",
    "when": "2026-03-06T10:00:00Z",
    "confirmed": true,
    "idempotency_key": "idem-1"
  }'
```

响应示例：
```json
{
  "ok": true,
  "data": {
    "job_id": "job-1",
    "audit": {
      "who": "alice",
      "when": "2026-03-06T10:00:00Z",
      "baseline_job_id": "job-base"
    }
  }
}
```

## 8. 导出与部署基准

### 8.1 创建导出任务
```bash
curl -s -X POST "http://127.0.0.1:8000/exports" \
  -H "Content-Type: application/json" \
  -d '{"job_id": "job-1", "run_id": "run-1", "backend": "tensorrt"}'
```

响应示例：
```json
{
  "ok": true,
  "data": {
    "export_id": "export-1"
  }
}
```

### 8.2 查询导出状态
```bash
curl -s "http://127.0.0.1:8000/exports/export-1"
```

### 8.3 查询部署基准
```bash
curl -s "http://127.0.0.1:8000/exports/export-1/deploy-benchmark"
```

响应示例：
```json
{
  "ok": true,
  "data": {
    "export_id": "export-1",
    "benchmark": {
      "throughput_fps": 120.0,
      "latency_p95_ms": 19.5,
      "memory_mb": 2048,
      "unit": {
        "throughput_fps": "fps",
        "latency_p95_ms": "ms",
        "memory_mb": "MB"
      }
    }
  }
}
```

## 9. 常见错误码示例
- `VALIDATION_ERROR`：请求参数非法或缺失
- `NOT_FOUND`：资源不存在
- `CONFLICT`：资源状态冲突
- `RESOURCE_LOCKED`：GPU 资源冲突
- `DATA_INVALID`：业务前置条件不满足
- `EXPORT_FAILED`：导出执行失败
