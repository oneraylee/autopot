# 1.Common库

## 1. Context Requirements（上下文依赖）
- 必读：`docs/init/2.系统架构.md`
- 必读：`docs/init/3.模块设计文档.md`
- 必读：`docs/init/5.附录.md`
- 选读：`docs/updates/20260323_知识库/1.知识库架构设计.md`

## 2. TDD Implementation Steps（TDD 执行步骤）
- **Step 1.1: 定义统一领域枚举与错误码**
  - `Task`: 建立任务状态、任务类型、精度模式、错误码（如 `TRAIN_OOM`、`EVAL_FAILED`）。
  - `Test File`: `tests/common/test_enums_and_error_codes.py`
  - `Impl File`: `src/common/domain_types.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 去重枚举值并补齐类型注解 (Refactor)

- **Step 1.2: 定义核心 JSON Schema 校验器**
  - `Task`: 提供 `EvalReport`、`EvidencePack`、`NextExperiments` 的 schema 校验入口。
  - `Test File`: `tests/common/test_schema_validators.py`
  - `Impl File`: `src/common/schema_validators.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 抽取公共校验错误格式 (Refactor)

- **Step 1.3: 定义路径与产物索引工具**
  - `Task`: 封装 `run_dir` 规范路径（weights/eval/evidence/llm/export）。
  - `Test File`: `tests/common/test_artifact_paths.py`
  - `Impl File`: `src/common/artifact_paths.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 统一路径拼装策略 (Refactor)

- **Step 1.4: 定义跨服务 DTO 与契约对象**
  - `Task`: 建立 `JobSpec`、`ProjectKpiConfig`、`EvalSummary` 等 DTO。
  - `Test File`: `tests/common/test_contract_dtos.py`
  - `Impl File`: `src/common/contracts.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 消除字段命名不一致 (Refactor)

- **Step 1.5: 定义知识系统与 Gateway 领域类型**
  - `Task`: 新增技能分类枚举（`SkillCategory`: training/model/data/eval_deploy）、技能层级枚举（`SkillLayer`）、成熟度枚举（`SkillMaturity`: draft/reviewed/verified/deprecated）、证据级别（`EvidenceLevel`）、LLM 调用类型枚举（`LLMCallType`: agent_plan/agent_diagnose/skill_extract/conflict_analyze/embedding）、冲突关系枚举（`RelationType`）、新增错误码 `LLM_GATEWAY_ERROR` 和 `KNOWLEDGE_IMPORT_FAILED`。
  - `Test File`: `tests/common/test_knowledge_domain_types.py`
  - `Impl File`: `src/common/domain_types.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 确保枚举值与知识库架构设计文档一致 (Refactor)

- **Step 1.6: 定义知识系统与 Gateway 契约对象**
  - `Task`: 新增 `SkillCardSummary`（技能摘要 DTO，用于在线检索输出）、`QuerySignature`（查询签名）、`ConflictReport`（冲突判定结果）、`SkillContext`（送入 Agent 的最小知识上下文）、`LLMCallRecord`（Gateway 调用记录 DTO）、`SkillRef`（实验方案中的技能引用）。同时在 `schema_validators.py` 中增加 `SkillCard` 和 `QuerySignature` 的 JSON Schema 校验入口。
  - `Test File`: `tests/common/test_knowledge_contracts.py`
  - `Impl File`: `src/common/contracts.py`, `src/common/schema_validators.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 与知识库架构设计 § 4.3.2 字段对齐 (Refactor)

## 3. Verification（验证标准）
- Common 层测试覆盖率 ≥ 90%。
- Schema 校验器可稳定拦截缺失必填字段与类型错误。
- 错误码、枚举、DTO 在后续层不再重复定义。
- 知识系统枚举值与 `1.知识库架构设计.md` 保持一致。
- `SkillCardSummary` / `QuerySignature` / `SkillContext` 可供业务层稳定使用。
