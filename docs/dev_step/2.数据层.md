# 2.数据层

## 1. Context Requirements（上下文依赖）
- 必读：`docs/init/1.产品PRD.md`
- 必读：`docs/init/3.模块设计文档.md`
- 必读：`docs/init/5.附录.md`
- 必读：`docs/updates/20260323_知识库/1.知识库架构设计.md`（§ 5 数据库表结构）

## 2. TDD Implementation Steps（TDD 执行步骤）
- **Step 2.1: 建立项目与 KPI 配置仓储**
  - `Task`: 实现 `Project`、`ProjectKpiConfig` 的存取与版本化读取。
  - `Test File`: `tests/repositories/test_project_repository.py`
  - `Impl File`: `src/repositories/project_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 统一查询接口返回模型 (Refactor)

- **Step 2.2: 建立数据集与版本仓储**
  - `Task`: 实现 `Dataset`、`DatasetVersion` 的创建、冻结、查询。
  - `Test File`: `tests/repositories/test_dataset_repository.py`
  - `Impl File`: `src/repositories/dataset_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 对齐版本冻结约束 (Refactor)

- **Step 2.3: 建立场景标签与覆盖率统计仓储**
  - `Task`: 支持 `SceneLabel` 写入与按维度/组合统计查询。
  - `Test File`: `tests/repositories/test_scene_label_repository.py`
  - `Impl File`: `src/repositories/scene_label_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 优化聚合查询接口 (Refactor)

- **Step 2.4: 建立任务与状态事件仓储**
  - `Task`: 实现 Job 元数据、状态流转事件、资源占用记录持久化。
  - `Test File`: `tests/repositories/test_job_repository.py`
  - `Impl File`: `src/repositories/job_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 补齐状态机合法流转校验 (Refactor)

- **Step 2.5: 建立产物索引仓储**
  - `Task`: 保存 run 产物索引（权重、评估、证据包、LLM 输出、导出产物）。
  - `Test File`: `tests/repositories/test_artifact_repository.py`
  - `Impl File`: `src/repositories/artifact_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 统一索引路径规范 (Refactor)

- **Step 2.6: 建立知识库表群仓储**
  - `Task`: 实现 `KnowledgeSource`、`KnowledgeDocument`、`KnowledgeChunk`、`Skill`、`TechniqueVersion`、`TechniqueCondition`、`TechniqueAction`、`TechniqueTradeoff`、`TechniqueRelation`、`TechniqueEvidence`、`TechniqueTemplate`、`TechniqueOutcome`、`PlanningSnapshot`、`RetrievalLog` 的存取。需支持：来源注册、文档导入、分块存储、技能 CRUD、关系图查询、条件过滤、outcome 回写、snapshot 记录。
  - `Test File`: `tests/repositories/test_knowledge_repository.py`
  - `Impl File`: `src/repositories/knowledge_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 索引与唯一约束对齐知识库设计 § 5，确保 `content_hash` 去重、`skill_code` 唯一 (Refactor)

- **Step 2.7: 建立 LLM 调用日志仓储**
  - `Task`: 实现 `LLMCallLog` 的写入与查询（按 call_type、provider、时间范围、成本统计）。支持 Gateway 调用完成后同步写入，以及 batch 查询用于 cost dashboard。
  - `Test File`: `tests/repositories/test_llm_call_log_repository.py`
  - `Impl File`: `src/repositories/llm_call_log_repository.py`
  - `Checklist`:
    - [ ] 编写失败的单元测试 (Red)
    - [ ] 实现最小代码通过测试 (Green)
    - [ ] 确保按 (call_type, created_at desc) 和 (provider, model) 索引存在 (Refactor)

## 3. Verification（验证标准）
- 数据层测试覆盖率 ≥ 85%。
- 关键实体（Project、DatasetVersion、Job、ArtifactIndex）可完整 CRUD。
- Job 状态流转非法路径被阻断并返回统一错误码。
- 知识库表群（knowledge_source / knowledge_document / knowledge_chunk / skill / technique_* / planning_snapshot / retrieval_log）可完整读写。
- `LLMCallLog` 可写入并按多维度查询。
- `content_hash` 去重、`skill_code` 唯一约束可生效。
